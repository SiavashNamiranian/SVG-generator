const Shape = require('/shapes.js');

describe('Shape', ()=>{

    describe('isCircle', () => {
        it('Should return true for Shape that has cx, cy, and r value and r value is less than cx and cy' () =>{
            const Shape = new circle(cx, cy, r);
            

        })
    })
})